import { Button } from "react-bootstrap";
import React from 'react';

const Yahoo = () => {
    return (
      <div className="container mt-5">
        <Button className="btn btn-primary ">Yahoo Authentication</Button>
      </div>
    );
};

export default Yahoo;