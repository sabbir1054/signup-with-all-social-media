import { Button } from "react-bootstrap";
import React from 'react';

const Microsoft = () => {
    return (
      <div className="container mt-5">
        <Button className="btn btn-primary">Microsoft Authentication</Button>
      </div>
    );
};

export default Microsoft;